config FREETZ_PACKAGE_DEV_TOOLS_AUTOCONF
	bool "Dev Tools autoconf (binary only)"
	depends on (FREETZ_PACKAGE_DEV_TOOLS && ! FREETZ_PACKAGE_DEV_TOOLS_MINIMAL)
	default n
	help
		development tools autoconf,
		needed by target toolchain.
