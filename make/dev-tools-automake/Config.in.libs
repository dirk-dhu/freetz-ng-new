config FREETZ_PACKAGE_DEV_TOOLS_AUTOMAKE
	bool "Dev Tools automake (binary only)"
	depends on (FREETZ_PACKAGE_DEV_TOOLS && ! FREETZ_PACKAGE_DEV_TOOLS_MINIMAL)
	default n
	help
		development tools automake,
		needed by target toolchain.
